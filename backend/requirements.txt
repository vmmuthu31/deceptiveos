# CipherNest Python Backend — all dependencies
# Install with: pip3 install -r requirements.txt

# SSH Honeypot
paramiko>=3.4.0

# FastAPI + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Auth
bcrypt>=4.1.0
PyJWT>=2.8.0

# Database
psycopg2-binary>=2.9.9

# System introspection (digital twin metadata)
psutil>=5.9.0

# Email validation
pydantic[email]>=2.0.0

# .env loading
python-dotenv>=1.0.0

# HTTP (optional richer client)
requests>=2.32.0
